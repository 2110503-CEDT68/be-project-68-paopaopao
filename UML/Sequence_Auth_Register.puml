@startuml Sequence_Auth_Register

header Car Rental Sequence Diagram
footer Page %page% of %lastpage%
title "Register User (POST /api/v1/auth/register)"

participant "Client" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:auth" as routerAuth
participant "<<controllers>>\n:auth" as controllersAuth
participant "<<model>>\n:User" as modelUser
database "<<MongoDB>>\n:users" as UsersDatabase

client -> server ++: req.post('/api/v1/auth/register')
server -> routerAuth ++: app.use('/api/v1/auth', auth)
routerAuth -> controllersAuth ++: register()
controllersAuth -> modelUser ++: User.create({name, tel, email, password, role})
modelUser -> UsersDatabase ++: insert user document
UsersDatabase --> modelUser --: user
modelUser --> controllersAuth --: user
controllersAuth --> client --: 200 {success: true, token}

deactivate routerAuth
deactivate server

@enduml
